<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.kitri.godinator.user.dao.UserDao">
  	<select id="idCheck" parameterType="String" resultType="int">
  		select count(*)
  		from member
  		where user_id = #{id}
  	</select>
  	
  	<insert id="register" parameterType="memberDto">
  		insert into member
  		(user_id, user_name, pass, email, email_domain, phone1, phone2, phone3, zipcode, address, address_detail, user_cate, withdraw, join_date)
  		values(#{userId}, #{userName}, #{pass}, #{email}, #{emailDomain}, #{phone1},
  				#{phone2}, #{phone3}, #{zipcode}, #{address}, #{addressDetail},#{userCate},'N', sysdate)
  	</insert>
  	
  	<insert id="mentorRegister" parameterType="map">
  		insert into mentor
  		(user_id, mentor, aprvl_date, cate)
  		values(#{userId}, 
  		, sysdate,'R')
  	</insert>
  	
  </mapper>